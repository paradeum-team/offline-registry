version: 0.1
log:
  level: info 
storage:
  filesystem:
    rootdirectory: /var/lib/registry
    maxthreads: 100
http:
    addr: --registry_ip--:5000
    secret: placeholder
    tls:
      certificate: /etc/registry/ssl/offlineregistry.offline-okd.com.crt
      key: /etc/registry/ssl/offlineregistry.offline-okd.com.key
